#include <stdio.h>
main(){
	float x,y;
	printf("Digite los valores de X y Y");
	if((x>=(-5/2)&&x<=2)&&(y>=(-11/2)&&(y<=4))){
		printf("El valor pertenece al %crea de la recta y la par%cbola",160,160);
	}else{
		printf("El valor no pertenece al %crea de la recta y la par%cbola",160,160);
	}
return 0;
}
